schema {
    query: Query
}

type Post {
    id: ID!
    title: String!
    created_at: String!
}

type PostResult {
    success: Boolean!
    error: [String]
    post: Post
}

type PostsResult {
    success: Boolean!
    error: [String]
    post: [Post]
}

type Query {
    listPosts: PostsResult!
    getPost(id: ID!): PostResult!
}